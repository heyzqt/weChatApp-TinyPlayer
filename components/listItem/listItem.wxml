<!--components/listItem/listItem.wxml-->
<view class="list-item-box" bindtap="handlePlayClick">
	<view class="profile">
		<image class="cover" src="{{pic}}"></image>
	</view>
	<view class="content">
		<view class="title">{{title}}</view>
		<view class="name">{{name}}</view>
	</view>
</view>